#include <stdio.h>
#include <math.h>

int main () {

  int i,j, n,m1,m2;
  scanf("%d",&n);
  int matriz[n][n];
  int mt[n][n];

  for(i=0; i < n; i++) {
    for(j=0; j < n; j++) {
      scanf("%d", &matriz[i][j]);
      mt[i][j] = matriz;
    }
  }

  for(i=0; i < n; i++) {

  }
  return 0;
}