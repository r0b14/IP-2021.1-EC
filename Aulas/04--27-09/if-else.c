#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int x=2, y=0;
    if (x > y) {
        printf("X eh maior que Y");
    } else {
        if (x == y) {
            printf("X eh igual a Y");
        } else  {
            printf("X eh menor que Y");
        }
        
    }
    return 0;
}