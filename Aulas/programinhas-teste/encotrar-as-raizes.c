#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/*int main()
{
    float a, b, c;
    float x1, x2, delta;
    printf("\nDigite os coeficientes a, b e c: ");
    scanf("%f %f %f", &a, &b, &c);

    if (a != 0) { 
        delta = pow(b, 2) - 4 * a * c
        if (delta >= 0) {
            if (delta > 0) {
                printf("x1 = %.2f, x2 = %.2f\n",
                (-b + sqrt(delta))/(2 * a),
                (-b + sqrt(delta))/(2 * a));
            } else {
                printf("Raiz unica = %.2f", -b /(2 * a));
            }
        }
    } else 
        printf("\n Isso nao eh uma equacao do 2o grau\n");

        return 0;
} */

/*

int main() {
    int x;
    printf("x = %d", x);
    return 0;
} */